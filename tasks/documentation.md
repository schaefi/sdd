# Request

Create python sphinx based documentation in a directory named doc using
the markup language ReST (Restructured Text). Take the spec information
to create the documentation from spec/documentation.md
